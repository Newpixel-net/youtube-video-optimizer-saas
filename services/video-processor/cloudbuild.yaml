# Cloud Build configuration for Video Processor Service
# Builds and deploys to Cloud Run

steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/video-processor:$BUILD_ID'
      - '-t'
      - 'gcr.io/$PROJECT_ID/video-processor:latest'
      - '.'

  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/video-processor:$BUILD_ID'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/video-processor:latest'

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'video-processor'
      - '--image'
      - 'gcr.io/$PROJECT_ID/video-processor:$BUILD_ID'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--memory'
      - '4Gi'
      - '--cpu'
      - '2'
      - '--timeout'
      - '900'
      - '--concurrency'
      - '1'
      - '--min-instances'
      - '0'
      - '--max-instances'
      - '10'
      - '--set-env-vars'
      - 'BUCKET_NAME=$PROJECT_ID.appspot.com,NODE_ENV=production,RAPIDAPI_KEY=${_RAPIDAPI_KEY}'
      - '--allow-unauthenticated'

images:
  - 'gcr.io/$PROJECT_ID/video-processor:$BUILD_ID'
  - 'gcr.io/$PROJECT_ID/video-processor:latest'

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'E2_HIGHCPU_8'

timeout: '1800s'

# Substitution variables (optional)
substitutions:
  _RAPIDAPI_KEY: ''
